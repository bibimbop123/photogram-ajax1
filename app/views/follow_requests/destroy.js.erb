<!--$("#<%#= dom_id(@follow_request) %>").toggle();-->

var current = $("<%= j(render "follow_requests/follow_unfollow", { :follow_request => @follow_request, :sender => @follow_request.sender, :recipient => @follow_request.recipient }) %>");
var update = $("#<%= dom_id(@follow_request) %>");
update.remove();

current.replaceWith(update)

console.log("You un-followed this user")
